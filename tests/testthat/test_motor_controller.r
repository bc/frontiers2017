context("test_motor_controller.r")
require(testthat)

source("../../R/settling_time_analysis.r")


test_that("data separates into 1000 forces for one posture", {
  idx_of_first_force_in_sep24_motor_test <- 5265
  # <- read.csv('')
})
